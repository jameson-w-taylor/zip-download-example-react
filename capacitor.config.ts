import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'zip-download-example-react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
